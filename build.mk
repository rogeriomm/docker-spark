Prj([Docker(name="base",        prefix="spark"),
     Docker(name="base-python", prefix="spark"),
     Docker(name="master",      prefix="spark"),
     Docker(name="worker",      prefix="spark"),
     Docker(name="submit",      prefix="spark")],
     "docker-spark")

